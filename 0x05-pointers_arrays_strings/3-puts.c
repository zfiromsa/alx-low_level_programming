#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 * _puts - prints a string, followed by \n, to stdout.
 * @*str: pointer to an char as parameter
 *
 * Return: void
 */

void _puts(char *str)
{
int i;
char st];

for (i = 0; ; i++)
{

if (str[i] != null)
{
st[i] = str[i];
}
else
{
st[i] = str[i];
}
}
puts(st);
}
