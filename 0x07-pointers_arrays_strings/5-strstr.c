#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 * _memcpy - func that locates a substring.
 * @s: first parm
 * @accept: second parm
 * return: len
 */

char *_strstr(char *haystack, char *needle)
{
int len;

len = strstr(s,accept);
return (len);
}
