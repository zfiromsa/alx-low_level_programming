#include "main.h"
#include <string.h>
/**
 * _strcmp - compares two strings.
 * @s2: first parm.
 * @s1: second parm.
 * Return: Always res.
 */

int _strcmp(char *s1, char *s2)
{
int res;

res = strcmp(s1, s2);
return (res);
}
