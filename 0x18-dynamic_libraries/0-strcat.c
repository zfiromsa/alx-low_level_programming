#include "main.h"
#include <string.h>
/**
 * _strcat - concatenates two strings.
 *
 * @dest: first parm
 * @src: second parm
 * Return: Always dest.
 */

char *_strcat(char *dest, char *src)
{
strcat(dest, src);
return (dest);
}
