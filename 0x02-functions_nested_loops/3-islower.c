#include "main.h"
/*
 * main - the checks for lowercase character.
 *@c: the main fuction take argument
 * Returns 1 if c is lowercase
 * Returns 0 otherwise
 */

int _islower(int c)
{
int ch;

ch = c;
if (ch == 67)
{
return (1);
}
return (0);
}
