#include <stdio.h>
#include "main.h"
/**
 * mul - multiplies two integers and
 * return the resualt return the resualt.
 * @a: First member
 * @b: Second member
 *
 * return: Alwayas(a * b).
 */

int mul(int a, int b)
{
return (a * b);
}
