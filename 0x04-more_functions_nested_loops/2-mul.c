#include <stdio.h>
#include "main.h"
/**
 * mul - multiplies two integers and
 * return the resualt return the resualt.
 * param a
 * param b
 * return: Alwayas(a * b).
 */

int mul(int a, int b)
{
return (a * b);
}
