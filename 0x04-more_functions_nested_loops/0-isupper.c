#include "main.h"
/*
 * _islower: - it is func checks for alphabetic character.
 * @c: it is an argumint.
 * Return: 1 0r 0.
 */

int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
return (0);
}
