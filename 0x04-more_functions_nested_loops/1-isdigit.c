#include <stdio.h>
#include "main.h"

/*
 * _islower: - it is func checks for alphabetic character.
 * @c: it is an argumint.
 * Return: 1 0r 0.
 */

int _isdigit(int c)
{
if ((c >= 48) && (c <= 57))
{
return (1);
}
else
{
return (0);
}
}
